<!DOCTYPE html>
<html lang="bn">
<head>
<meta charset="UTF-8">
<title>রিভিউ সাবমিশন - এডমিন প্যানেল</title>
<link rel="stylesheet" href="/style.css">
<style>
.admin-header {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
    padding: 20px;
    text-align: center;
    margin-bottom: 30px;
}

.nav-menu {
    display: flex;
    justify-content: center;
    gap: 15px;
    margin: 20px 0;
    flex-wrap: wrap;
}
.nav-box {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
    padding: 10px 20px;
    border-radius: 8px;
    text-decoration: none;
    font-weight: bold;
    transition: transform 0.3s;
    cursor: pointer;
    font-size: 14px;
}
.nav-box:hover {
    transform: translateY(-2px);
    color: white;
}

.review-container {
    padding: 20px;
}

.review-card {
    background: white;
    border-radius: 10px;
    padding: 20px;
    margin-bottom: 20px;
    box-shadow: 0 5px 15px rgba(0,0,0,0.1);
    border-left: 4px solid #f39c12;
}

.review-card.approved {
    border-left-color: #28a745;
}

.review-card.rejected {
    border-left-color: #dc3545;
}

.review-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 15px;
    flex-wrap: wrap;
}

.review-info {
    flex: 1;
}

.review-status {
    padding: 8px 15px;
    border-radius: 20px;
    font-size: 12px;
    font-weight: bold;
    margin-left: 10px;
}

.status-pending {
    background: #fff3cd;
    color: #856404;
}

.status-approved {
    background: #d4edda;
    color: #155724;
}

.status-rejected {
    background: #f8d7da;
    color: #721c24;
}

.review-details {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 15px;
    margin: 15px 0;
}

.detail-item {
    background: #f8f9fa;
    padding: 10px;
    border-radius: 5px;
}

.detail-label {
    font-weight: bold;
    color: #666;
    font-size: 12px;
    text-transform: uppercase;
}

.detail-value {
    color: #333;
    font-size: 14px;
    margin-top: 5px;
}

.review-message {
    background: #f8f9fa;
    padding: 15px;
    border-radius: 8px;
    margin: 15px 0;
    border-left: 3px solid #667eea;
}

.message-text {
    color: #333;
    line-height: 1.6;
    font-style: italic;
}

.screenshot-section {
    margin: 15px 0;
}

.screenshot-preview {
    max-width: 300px;
    border-radius: 8px;
    border: 2px solid #ddd;
    cursor: pointer;
    transition: transform 0.3s;
}

.screenshot-preview:hover {
    transform: scale(1.05);
}

.review-actions {
    display: flex;
    gap: 10px;
    margin-top: 15px;
    flex-wrap: wrap;
}

.action-btn {
    padding: 8px 15px;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    font-weight: bold;
    text-decoration: none;
    text-align: center;
    transition: transform 0.3s;
}

.action-btn:hover {
    transform: translateY(-2px);
}

.btn-approve {
    background: #28a745;
    color: white;
}

.btn-reject {
    background: #dc3545;
    color: white;
}

.btn-view {
    background: #007bff;
    color: white;
}

.stats-container {
    display: flex;
    justify-content: space-around;
    margin: 20px 0;
    flex-wrap: wrap;
}

.stat-box {
    background: #f8f9fa;
    padding: 20px;
    border-radius: 10px;
    text-align: center;
    min-width: 150px;
    margin: 10px;
}

.stat-number {
    font-size: 24px;
    font-weight: bold;
    color: #667eea;
    margin: 10px 0;
}

.filter-section {
    background: white;
    padding: 20px;
    border-radius: 10px;
    margin-bottom: 20px;
    box-shadow: 0 5px 15px rgba(0,0,0,0.1);
}

.filter-buttons {
    display: flex;
    gap: 10px;
    flex-wrap: wrap;
}

.filter-btn {
    padding: 8px 15px;
    border: 2px solid #667eea;
    background: white;
    color: #667eea;
    border-radius: 20px;
    cursor: pointer;
    transition: all 0.3s;
}

.filter-btn.active, .filter-btn:hover {
    background: #667eea;
    color: white;
}

/* Modal styles for image preview */
.modal {
    display: none;
    position: fixed;
    z-index: 1000;
    left: 0;
    top: 0;
    width: 100%;
    height: 100%;
    background-color: rgba(0,0,0,0.9);
}

.modal-content {
    margin: auto;
    display: block;
    width: 80%;
    max-width: 700px;
    max-height: 80%;
    border-radius: 10px;
}

.close {
    position: absolute;
    top: 15px;
    right: 35px;
    color: #f1f1f1;
    font-size: 40px;
    font-weight: bold;
    cursor: pointer;
}

.close:hover {
    color: #bbb;
}
</style>
</head>
<body>
<div class="admin-header">
    <h2>রিভিউ সাবমিশন ম্যানেজমেন্ট</h2>
    <p>ইউজারদের রিভিউ দেখুন এবং অনুমোদন/বাতিল করুন</p>
</div>

<div class="nav-menu">
    <a href="/admin_panel" class="nav-box">🏠 মূল প্যানেল</a>
    <a href="/admin/user_information" class="nav-box">👥 ইউজার তথ্য</a>
    <a href="/admin/payment_records" class="nav-box">💰 পেমেন্ট রেকর্ড</a>
    <a href="/admin/review_submissions" class="nav-box" style="background: linear-gradient(135deg, #28a745 0%, #20c997 100%);">⭐ রিভিউ সাবমিশন</a>
    <a href="/admin/messaging_system" class="nav-box">💬 মেসেজিং</a>
</div>

<div class="stats-container">
    <div class="stat-box">
        <h4 style="color: #667eea; margin: 0;">মোট রিভিউ</h4>
        <div class="stat-number"><%= reviews.length %></div>
    </div>
    <div class="stat-box">
        <h4 style="color: #f39c12; margin: 0;">পেন্ডিং</h4>
        <div class="stat-number"><%= reviews.filter(r => r.status === 'pending').length %></div>
    </div>
    <div class="stat-box">
        <h4 style="color: #28a745; margin: 0;">অনুমোদিত</h4>
        <div class="stat-number"><%= reviews.filter(r => r.status === 'approved').length %></div>
    </div>
    <div class="stat-box">
        <h4 style="color: #dc3545; margin: 0;">বাতিল</h4>
        <div class="stat-number"><%= reviews.filter(r => r.status === 'rejected').length %></div>
    </div>
</div>

<div class="filter-section">
    <h4 style="margin-bottom: 15px;">ফিল্টার করুন:</h4>
    <div class="filter-buttons">
        <button class="filter-btn active" onclick="filterReviews('all')">সব দেখুন</button>
        <button class="filter-btn" onclick="filterReviews('pending')">পেন্ডিং</button>
        <button class="filter-btn" onclick="filterReviews('approved')">অনুমোদিত</button>
        <button class="filter-btn" onclick="filterReviews('rejected')">বাতিল</button>
    </div>
</div>

<div class="review-container">
    <% if (reviews.length === 0) { %>
        <div style="text-align: center; padding: 50px; color: #666;">
            <h3>কোনো রিভিউ পাওয়া যায়নি</h3>
            <p>এখনো কোনো ইউজার রিভিউ পাঠায়নি।</p>
        </div>
    <% } else { %>
        <% reviews.forEach(review => { %>
            <div class="review-card <%= review.status %>" data-status="<%= review.status %>">
                <div class="review-header">
                    <div class="review-info">
                        <h3 style="margin: 0; color: #333;"><%= review.userName %></h3>
                        <p style="margin: 5px 0; color: #666; font-size: 14px;">Review ID: #<%= review.id %></p>
                    </div>
                    <div class="review-status status-<%= review.status %>">
                        <%= review.status === 'pending' ? 'অপেক্ষমান' : 
                            review.status === 'approved' ? 'অনুমোদিত' : 'বাতিল' %>
                    </div>
                </div>
                
                <div class="review-details">
                    <div class="detail-item">
                        <div class="detail-label">রিটার্ন নম্বর</div>
                        <div class="detail-value"><%= review.returnNumber %></div>
                    </div>
                    <div class="detail-item">
                        <div class="detail-label">ইউজার ইমেইল</div>
                        <div class="detail-value"><%= review.userEmail %></div>
                    </div>
                    <div class="detail-item">
                        <div class="detail-label">ইউজার ফোন</div>
                        <div class="detail-value"><%= review.userPhone %></div>
                    </div>
                    <div class="detail-item">
                        <div class="detail-label">জমা দেওয়ার সময়</div>
                        <div class="detail-value"><%= new Date(review.submittedAt).toLocaleString('bn-BD') %></div>
                    </div>
                    <% if (review.status === 'approved' && review.approvedAt) { %>
                    <div class="detail-item">
                        <div class="detail-label">অনুমোদনের সময়</div>
                        <div class="detail-value"><%= new Date(review.approvedAt).toLocaleString('bn-BD') %></div>
                    </div>
                    <% } %>
                    <% if (review.status === 'rejected' && review.rejectedAt) { %>
                    <div class="detail-item">
                        <div class="detail-label">বাতিলের সময়</div>
                        <div class="detail-value"><%= new Date(review.rejectedAt).toLocaleString('bn-BD') %></div>
                    </div>
                    <% } %>
                </div>
                
                <% if (review.message) { %>
                <div class="review-message">
                    <div class="detail-label">রিভিউ মেসেজ:</div>
                    <div class="message-text">"<%= review.message %>"</div>
                </div>
                <% } %>
                
                <% if (review.screenshot) { %>
                <div class="screenshot-section">
                    <div class="detail-label">স্ক্রিনশট:</div>
                    <img src="/uploads/<%= review.screenshot %>" 
                         alt="Review Screenshot" 
                         class="screenshot-preview" 
                         onclick="openModal('/uploads/<%= review.screenshot %>')">
                </div>
                <% } %>
                
                <% if (review.status === 'pending') { %>
                <div class="review-actions">
                    <form method="POST" action="/admin/approve_review/<%= review.id %>" style="display: inline;">
                        <button type="submit" class="action-btn btn-approve" 
                                onclick="return confirm('এই রিভিউটি অনুমোদন করতে চান?')">
                            ✅ অনুমোদন করুন
                        </button>
                    </form>
                    <form method="POST" action="/admin/reject_review/<%= review.id %>" style="display: inline;">
                        <button type="submit" class="action-btn btn-reject" 
                                onclick="return confirm('এই রিভিউটি বাতিল করতে চান?')">
                            ❌ বাতিল করুন
                        </button>
                    </form>
                </div>
                <% } %>
            </div>
        <% }); %>
    <% } %>
</div>

<div style="text-align: center; margin: 30px 0;">
    <a href="/admin_panel" class="btn btn-secondary">এডমিন প্যানেলে ফিরে যান</a>
    <a href="/admin_logout" class="btn btn-danger" style="margin-left: 15px;">লগআউট</a>
</div>

<!-- Image Modal -->
<div id="imageModal" class="modal">
    <span class="close">&times;</span>
    <img class="modal-content" id="modalImage">
</div>

<script>
function filterReviews(status) {
    const cards = document.querySelectorAll('.review-card');
    const buttons = document.querySelectorAll('.filter-btn');
    
    // Update active button
    buttons.forEach(btn => btn.classList.remove('active'));
    event.target.classList.add('active');
    
    // Filter cards
    cards.forEach(card => {
        if (status === 'all') {
            card.style.display = 'block';
        } else {
            if (card.dataset.status === status) {
                card.style.display = 'block';
            } else {
                card.style.display = 'none';
            }
        }
    });
}

// Image modal functions
function openModal(imageSrc) {
    const modal = document.getElementById('imageModal');
    const modalImg = document.getElementById('modalImage');
    
    modal.style.display = 'block';
    modalImg.src = imageSrc;
}

// Close modal when clicking on X
document.querySelector('.close').onclick = function() {
    document.getElementById('imageModal').style.display = 'none';
}

// Close modal when clicking outside image
window.onclick = function(event) {
    const modal = document.getElementById('imageModal');
    if (event.target == modal) {
        modal.style.display = 'none';
    }
}

// Page load notifications
window.onload = function() {
    console.log('রিভিউ সাবমিশন পেজ লোড হয়েছে');
    
    // Auto-refresh every 30 seconds for new reviews
    setInterval(() => {
        console.log('রিভিউ স্ট্যাটাস চেক করা হচ্ছে...');
        // location.reload(); // Uncomment if you want auto-refresh
    }, 30000);
};
</script>
</body>
</html>